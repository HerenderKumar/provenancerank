# Where the UI sends API calls. Default "/api" is proxied to the backend by
# vite (dev) and nginx (prod), so you usually don't need to change this.
VITE_API_BASE=/api

# Dev-only: where vite proxies /api during `npm run dev`
VITE_API_TARGET=http://localhost:8000
